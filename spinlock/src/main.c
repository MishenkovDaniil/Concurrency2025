#if 0
include <stdio.h>

#include "spinlock_tas.h"
#include "spinlock_ttas.h"
#include "ticket_lock.h"
#endif

int main(int argc, char** argv)
{
    // there should be example of usage;
    // but TAS, TTAS, ticket-lock have the similar api as its C-brothers
    return 0;
}